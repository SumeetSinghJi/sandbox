Description changes

* repo description typo a suggestion is: vedic-lang the first Turing complete Sanskrit Programming language
* Include in documentation: tab indentation rule e.g. 2 spaces or 4 spaces
* Footer in documentation says "Devloped". It should say “Developed”
* Could you possible write in documentation मान is Type Inference. The compiler automatically deduces
the type based on the variables value.

Code changes

* How to string concatenate e.g, वद("परिणामः " + अ)
* need to interface with host platform so we can create libs for Random, Sleep, Window
* need a poll key event
* All vada (cout) statements have new line. Can we instead have a special character for new lines?